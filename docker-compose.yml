version: '3.8'

services:
  web:
    build: .
    container_name: paffloat_app
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped